BellmanFord(G, V, E, source)

for each vertex v
    dist[v] = ∞

dist[source] = 0

for i = 1 to V-1
    for each edge (u,v,w)
        if dist[u] != ∞ and
           dist[u] + w < dist[v]
        then
            dist[v] = dist[u] + w

for each edge (u,v,w)
    if dist[u] != ∞ and
       dist[u] + w < dist[v]
    then
        print "Negative Cycle Exists"
        return

print distances